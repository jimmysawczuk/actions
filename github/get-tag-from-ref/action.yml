name: "Get tag from github.ref"
description: "Get the tag from github.ref"
author: "Jimmy Sawczuk"
inputs:
  ref:
    description: "The ref to clean"
    required: true
outputs:
  tag:
    description: "The tag, if present"
  branch:
    description: "The branch, if present"
runs:
  using: "node16"
  main: "dist/main.js"
branding:
  icon: "filter"
  color: "gray-dark"
