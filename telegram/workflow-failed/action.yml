name: "Send Workflow Failed Message"
description: "Send Telegram Message"
author: "Jimmy Sawczuk"
inputs:
  chatID:
    description: "The chat ID to send the message to"
    required: true
  telegramBotURL:
    description: "The URL of the sendMessage endpoint for your bot"
    required: true
runs:
  using: "node12"
  main: "dist/index.js"
branding:
  icon: "filter"
  color: "gray-dark"
